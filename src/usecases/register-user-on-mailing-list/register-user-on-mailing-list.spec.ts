/* eslint-disable @typescript-eslint/require-await */
import { UserData } from './user-data'

describe('Register user on mailing list', () => {
  test('should add user with complete data to mailing list', () => {
    const users: UserData[] = []
    console.log(users)
    // const repo: UserRepository = new InMemoryUserRepository(user)
    // const usecase: RegisterUserOrMailingList = new RegisterUserOnMailngList(repo)
    // const name ='any_name'
    // const email = 'any@mail.com'
    // const response = await usecase.registerUserOnMailngList({ name, email })
    // const user = repo.findUserByEmail('any@mail.com')
    // expect((await user).name).toBe('any_name')
  })
})
